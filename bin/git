#!/bin/zsh

case $1 in
  cd   ) cd "$(ghq list --full-path --exact `ghq list | fzf`)" ;;
  root ) cd "$(git rev-parse --show-toplevel)" ;;
  *    ) if type hub >/dev/null 2>&1; then hub "$@"; else git "$@"; fi ;;
esac

